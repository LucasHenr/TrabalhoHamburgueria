<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.cotemig.juliano.trabalho">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Trabalho">
        <activity
            android:name=".ul.activities.PedidoFinalizado"
            android:exported="true" />
        <activity
            android:name=".ul.activities.PagamentoActivity"
            android:exported="true" />
        <activity
            android:name=".ul.activities.DescricaoActivity"
            android:exported="true" />
        <activity
            android:name=".ul.activities.RegisterActivity"
            android:exported="true" />
        <activity
            android:name=".ul.activities.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".ul.activities.WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ul.activities.MainActivity"
            android:exported="true" />
    </application>

</manifest>